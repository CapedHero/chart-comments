// Base unit makes it much easier to tweak the size of the chart.
const BASE_UNIT = 100;

const CHART_SIZES = {
  height: BASE_UNIT * 3,
  marginBottom: BASE_UNIT * 0.4,
  marginLeft: BASE_UNIT * 0.25,
  marginRight: BASE_UNIT * 0.25,
  marginTop: BASE_UNIT * 0.5,
  width: BASE_UNIT * 4,
};

export { CHART_SIZES };
